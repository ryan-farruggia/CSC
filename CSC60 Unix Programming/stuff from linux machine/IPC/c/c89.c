#include <stdio.h>
int main ( )
{

  for ( int i = 0 ; i < 5 ; i++ )
   putchar ( 'A' + i);
   putchar ( '\n' );


  double x;
  long double y;

  printf ( "%d %d \n", sizeof ( x), sizeof ( y) );




}
