#include <stdio.h>
int main ( ) 
{
  // Complete this open functio 
  int fd = open ( "numbers.txt", "w");
  char buf[1000] = " Hello I am in csc 60 "
  write ( fd, buf, strlen  (buf);
  close ( fd);
}

  
